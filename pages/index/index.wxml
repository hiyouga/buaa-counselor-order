<!--index.wxml-->
<view class="container">
  <view class="navbar">
    <button class="navbtn" wx:for="{{navbar}}" data-index="{{index}}" data-href="{{navurl[index]}}" wx:key="unique" bindtap="navbarTap">{{item}}</button>
  </view>
</view>
